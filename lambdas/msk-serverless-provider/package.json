{
  "name": "@matano/msk-serverless-provider",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && concurrently 'esbuild msk-serverless-handler.ts --platform=node --outfile=dist/output.js --bundle --loader:.ts=ts' 'tsc --noEmit'",
    "watch": "tsc -w",
    "test": "jest"
  },
  "devDependencies": {
    "concurrently": "^7.2.2",
    "esbuild": "^0.14.48",
    "@types/aws-lambda": "^8.10.77",
    "@types/cfn-response": "^1.0.4",
    "@types/jest": "^26.0.10",
    "@types/node": "^18.0.1",
    "@types/prettier": "^2.6.3",
    "aws-cdk": "^2.28",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.7"
  },
  "prettier": {
    "printWidth": 120
  },
  "dependencies": {
    "aws-sdk": "^2.1161.0",
    "cfn-response": "1.0.1",
    "dayjs": "1.10.4",
    "kafkajs": "^2.2.0-beta.0"
  }
}
